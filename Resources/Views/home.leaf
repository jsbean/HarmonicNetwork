<html>
<body>
  <style>
  /* Style the submit button */
  button[type=submit] {
    width: 61.8%;
    padding: 14px 20px;
    margin: 8px 0;
    border: none;
    border-radius: 4px;
    cursor: pointer;
  }
  .buttons {
    text-align: center;
  }
  </style>
  <h1>Harmonic Network</h1>
  <h2>Find your way through a network of harmonies! (Don't get lost…)</h2>
  <h2>Path: #(path)</h2>
  <form method="post" action="/add">
    <div class="buttons">
      #for(neighborInfo in neighbors) {
        <button type="submit" name="#(path) #(neighborInfo.node)">#(neighborInfo.node) #(neighborInfo.probability)</button>
      }
    </div>
  </form>
</body>
</html>
