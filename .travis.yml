os:
  - osx
  - linux
language: generic
sudo: required
dist: trusty
osx_image: xcode10.2
install:
  - eval "$(curl -sL https://swiftenv.fuller.li/install.sh)"
env:
  - 4.2
script:
  - swift package update
  - swift test
